# Лабораторная работа №4  
**Тема:** Проектирование REST API  
**Цель работы:** Получить опыт проектирования программного интерфейса
## Документация по API  
### Выбор сервиса для проектирования REST API  
В качестве сервиса, для которого будет разработано API, был выбран микросервис "Electronic Types", его можно найти на диаграмме контейнеров:  
<img width="2021" height="991" alt="изображение" src="https://github.com/user-attachments/assets/8fd15712-be78-4894-a3be-4812537d3091" />  
### Метод GET /v1/ElectronicTypes  
Получить список видов электроники с суммой выдачи (в рублях) за их сдачу и id прикрепленных к ним документов - правил проверки и оценки.  
Список сортируется по возрастанию id возвращаемых видов электроники.  
Если список видов электроники изменится, то возможен пропуск элементов при их постраничном просмотре. Чтобы избежать этого, можно проверять, изменилась ли коллекция в ходе итераций при помощи ETag.  
#### Описание запроса
##### Параметры запроса  
- skip: integer, необязательный, неотрицательное - число пропущенных с начала списка элементов, при его сортировке по id (максимально можно вернуть 100 элементов за запрос, по умолчанию равно 0).  
- maxCount: integer, необязательный, неотрицательное - максимальное число возвращённых с начала списка элементов после пропуска skip, при его сортировке по id (максимально можно вернуть 100(если maxCount больше 100, то всё равно выдастся только 100 элементов) элементов за запрос, по умолчанию равно 10).  
- select: string, необязательный - список возвращаемых полей объектов через запятую. При пропуске, вернёт все поля, доступные текущему пользователю по его роли (для клиента это поля id,name,ETag,price, для инспектора и администратора к этим полям добавляется поле rule). При указании поля, недоступного текущему пользователю, ответ будет содержать ошибку 403 Forbidden.      
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- If-None-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер вернёт список только если посланный ETag данной коллекции не совпадает с ETag данной коллекции на сервере (иначе будет ответ с кодом 304 Not Modified).  
- If-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер вернёт список только если посланный ETag данной коллекции совпадает с ETag данной коллекции на сервере (иначе будет ответ с кодом 412 Precondition Failed).  
##### Тело запроса  
Так как это метод GET, то тело запроса следует оставлять пустым.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен)  
- 400 Bad Request - неверный формат заголовков запроса или неверное значение параметров запросов (не целое неотрицательное число в случае skip и maxCount или несуществующее поле объекта в случае select), либо неизвестный параметр запроса, либо запрос не является корректным HTTP запросом    
- 403 Forbidden - в select указаны поля, недоступные для текущего пользователя, в теле ответа вернётся список этих полей.    
- 304 Not Modified - при указании заголовка If-None-Match, если значение заголовка совпадает с ETag данной коллекции на сервере  
- 412 Precondition failed - при указании заголовка If-Match, если значение заголовка не совпадает с ETag данной коллекции на сервере  
- 200 OK - если получилось успешно вернуть требуемые элементы коллекции (возможно, их будет 0, при пустой коллекции или при некоторых параметрах skip и maxCount).  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 200 OK, равен application/json.
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.  
- ETag: string - текущий ETag всей коллекции ElectronicTypes на стороне сервера (при ответе с кодом 200. Иначе не возвращается). ETag коллеции представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление элемента в коллекции.  
##### Тело ответа  
При коде ответа 200 OK, вернётся JSON объект с 3 полями:  
- count: integer - число возвращённых видов электроники  
- totalCount: integer - суммарное число видов электроники в коллекции  
- items: array - список с возвращёнными видами электроники.  
  
Каждый элемент в массиве items представляет собой объект JSON с некоторыми из следующих полей (в зависимости от роли пользователя в системе и значения параметра "select"):  
- id: integer - уникальный идентификатор вида электроники  (доступно всем авторизованным пользователям)  
- name: string - название вида электроники  (доступно всем авторизованным пользователям)  
- ETag: string - текущий ETag данного вида электроники (доступно всем авторизованным пользователям)  
- rule: string - название документа без URL-encoding с правилами проверки и оценки для данного элемента (доступно только ролям "администратор" и "инспектор" системы). Если документ для данного вида электроники отсутствует, то данное поле возвращено не будет  
- price: integer - сумма денег (в рублях), получаемых клиентом за сдачу данного вида электроники (доступно всем авторизованным пользователям). Если сумма для данного вида электроники не указана, то данное поле возвращено не будет  
Иначе, в теле ответа могут содержаться сведения об ошибке, дублирующие или дополняющие код ответа.  
#### Примеры
##### Пример 1 - пользователь не указал токен входа в систему. 
Запрос:  
```
GET /v1/ElectronicTypes?unknownParam=true HTTP/1.1

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен). 
Запрос:  
```
GET /v1/ElectronicTypes?unknownParam=true HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии. 
Запрос:  
```
GET /v1/ElectronicTypes?unknownParam=true HTTP/1.1
Authorization: Bearer unknownToken
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент указал неизвестный параметр в строке запроса. 
Запрос:  
```
GET /v1/ElectronicTypes?unknownParam=true HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found. Only allowed parameteres are: skip,maxCount,select"}
```
##### Пример 5 - пользователь-клиент указал в параметрах skip и maxCount не целое неотрицательное число. 
Запрос:  
```
GET /v1/ElectronicTypes?skip=-80&maxCount=-80 HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Wrong skip parameter value. Skip value should be non-negative integer"}
```
##### Пример 6 - пользователь-клиент указал в параметре maxCount не целое неотрицательное число. 
Запрос:  
```
GET /v1/ElectronicTypes?maxCount=-80 HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Wrong maxCount parameter value. MaxCount value should be non-negative integer"}
```
##### Пример 7 - пользователь-клиент указал в параметре select неизвестное название поля. 
Запрос:  
```
GET /v1/ElectronicTypes?select=v,u HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown select fields. Allowed select fields are: id,name,rule,price"}
```
##### Пример 8 - пользователь-клиент указал в параметре select поле rule, недоступное для клиентов. 
Запрос:  
```
GET /v1/ElectronicTypes?select=id,rule HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to select fields: rule"}
```
##### Пример 9 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 304 Not Modified
Content-Type: application/json

{"code":304,"message":"ETag is equal to If-None-Match value"}
```
##### Пример 10 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ (правила оценки для единственного вида электроники отсутствуют):  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "3"

{"count":1,"totalCount":1,"items":[{"id":0, "name":"Apple Iphone 6","ETag":"1","price":5000}]}
```
##### Пример 11 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ (правила оценки для элемента с id=0 отсутствует, вознаграждение за отдачу не указано для элементов с id=0,1):  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "10"

{"count":10,"totalCount":10,"items":[{"id":0, "name":"Apple Iphone 6","ETag":"1"},{"id":1, "name":"Apple Iphone 7","ETag":"1",rule="apple_iphone_7_rules.pdf"},{"id":2, "name":"Apple Iphone 8","ETag":"1",rule="apple_iphone_8_rules.pdf","price":7000},{"id":3, "name":"Apple Iphone X","ETag":"1",rule="apple_iphone_X_rules.pdf","price":10000},{"id":4, "name":"Apple Iphone 11","ETag":"1",rule="apple_iphone_11_rules.pdf","price":11000},{"id":5, "name":"Apple Iphone 12","ETag":"1",rule="apple_iphone_12_rules.pdf","price":12000},{"id":6, "name":"Apple Iphone 13","ETag":"1",rule="apple_iphone_13_rules.pdf","price":13000},{"id":7, "name":"Apple Iphone 14","ETag":"1",rule="apple_iphone_14_rules.pdf","price":14000},{"id":8, "name":"Apple Iphone 15","ETag":"1",rule="apple_iphone_15_rules.pdf","price":15000},{"id":9, "name":"Apple Iphone 16","ETag":"1",rule="apple_iphone_16_rules.pdf","price":16000}]}
```
##### Пример 12 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ:  
```
HTTP/1.1 412 Precondition Failed
Content-Type: application/json

{"code":412,"message":"ETag is not equal to If-Match value"}
```
##### Пример 13 - Пользователь-клиент корректно указал значения параметров "select", "skip", "maxCount"  
Запрос:  
```
GET /v1/ElectronicTypes?select=id,name&skip=10&maxCount=3 HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "800"

{"count":3,"totalCount":117,"items":[{"id":10,"name":"Google Pixel"},{"id":11,"name":"Google Pixel 2"},{"id":12,"name":"Google Pixel 3"}]}
```
### Метод GET /v1/ElectronicTypes/{id}  
Получить вид электроники с суммой выдачи за его сдачу и имя прикреплённого к нему документа - правил проверки и оценки (сумма выдачи за сдачу price и имя прикреплённого документа rule вернутся, только если они присутствуют в системе для этого вида электроники)  
При запросе можно использовать заголовок If-None-Match с указанием ETag, чтобы вернуть вид электроники только в том случае, если он был обновлён.  
ETag вида электроники на сервере увеличивается каждый раз при его изменении (методы для изменения вида электроники пока не реализованы), удалении или добавлении(методы для добавления вида электроники по id пока не реализованы, а при помощи POST будет добавляться всегда вид электроники с id, который до этого никогда не использовалось(при помощи автоинкремента)).  
#### Описание запроса  
##### Параметры запроса  
- id: integer - уникальный идентификатор вида электроники  
- select: string, необязательный - список возвращаемых полей вида электроники через запятую. При пропуске, вернёт все поля, доступные текущему пользователю по его роли (для клиента это поля id,name,price,ETag, для инспектора и администратора к этим полям добавляется поле rule). При указании поля, недоступного текущему пользователю, ответ будет содержать ошибку 403 Forbidden.  
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- If-None-Match: string, необязательный - принимает значение ETag в двойных кавычках. При указании, делает так, что сервер вернёт вид электроники только если посланный ETag данного вида электроники не совпадает с ETag данного вида электроники на сервере (иначе будет ответ с кодом 304 Not Modified).  
##### Тело запроса  
Так как это метод GET, то тело запроса следует оставлять пустым.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен)  
- 400 Bad Request - неверный формат заголовков запроса или неверное значение параметров запросов (несуществующее поле объекта в случае select), либо неизвестный параметр запроса, либо запрос не является корректным HTTP запросом
- 404 Not Found - если вид электроники с заданным id не найден на сервере.  
- 403 Forbidden - в select указаны поля, недоступные для текущего пользователя, в теле ответа вернётся список этих полей.  
- 304 Not Modified - при указании заголовка If-None-Match, если значение заголовка совпадает с ETag данного вида электроники на сервере  
- 200 OK - если получилось успешно вернуть поля из вида электроники с данным id.  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 200 OK, равен application/json.
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.  
- ETag: string - текущий ETag данного вида электроники на стороне сервера (при ответе с кодом 200. Иначе не возвращается). ETag вида электроники представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление вида электроники с заданным id.  
##### Тело ответа  
При коде ответа 200 OK, возвращается объект JSON со всеми или несколькими из следующих полей (список возвращаемых полей зависит от роли пользователя и значения параметра select):  
- id: integer - уникальный идентификатор вида электроники  (доступно всем авторизованным пользователям)  
- name: string - название вида электроники  (доступно всем авторизованным пользователям)
- ETag: string - текущий ETag данного вида электроники (доступно всем авторизованным пользователям), дублирует значение заголовка "ETag"  
- rule: string - название документа без URL-encoding с правилами проверки и оценки для данного элемента (доступно только ролям "администратор" и "инспектор" системы). Если документ для данного вида электроники отсутствует, то данное поле возвращено не будет  
- price: integer - сумма денег (в рублях), получаемых клиентом за сдачу данного вида электроники (доступно всем авторизованным пользователям). Если сумма для данного вида электроники не указана, то данное поле возвращено не будет
Иначе, в теле ответа могут содержаться сведения об ошибке, дублирующие или дополняющие код ответа.  
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему.  
Запрос:  
```
GET /v1/ElectronicTypes/1?unknownParam=true HTTP/1.1

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен).  
Запрос:  
```
GET /v1/ElectronicTypes/1?unknownParam=true HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии.  
Запрос:  
```
GET /v1/ElectronicTypes/1?unknownParam=true HTTP/1.1
Authorization: Bearer unknownToken
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент указал неизвестный параметр в строке запроса.  
Запрос:  
```
GET /v1/ElectronicTypes/1?unknownParam=true HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found. Only allowed parameteres are: select"}
```
##### Пример 5 - пользователь-клиент указал в параметре select неизвестное название поля. 
Запрос:  
```
GET /v1/ElectronicTypes/1?select=v,u HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown select fields. Allowed select fields are: id,name,rule,price,ETag"}
```
##### Пример 6 - пользователь-клиент указал в параметре select поле rule, недоступное для клиентов. 
Запрос:  
```
GET /v1/ElectronicTypes/1?select=id,rule HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to select fields: rule"}
```
##### Пример 7 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes/21 HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 304 Not Modified
Content-Type: application/json

{"code":304,"message":"ETag is equal to If-None-Match value"}
```
##### Пример 8 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/ElectronicTypes/21 HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "0"
```
Ответ (правила оценки для 21-го вида электроники отсутствуют):  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "1"

{"id":21, "name":"Apple Watch Series 2","ETag":"1","price":5000}
```
##### Пример 9 - пользователь-администратор указал в заголовке id несуществующего объекта  
Запрос:  
```
GET /v1/ElectronicTypes/1000?select=id,name HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
```
Ответ:  
```
HTTP/1.1 404 Not Found
Content-Type: application/json

{"code":404,"message":"Electronic type with this id is not found"}
```
##### Пример 10 - Пользователь-клиент корректно указал значение параметра "select"  
Запрос:  
```
GET /v1/ElectronicTypes/25?select=id,name HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "1"

{"id":25,"name":"Apple Watch Series 4"}
```
##### Пример 11 - Пользователь-клиент не указал значение параметра "select" для вида электроники, имеющего все поля (включая price и rule)  
Запрос:  
```
GET /v1/ElectronicTypes/25 HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "1"

{"id":25,"name":"Apple Watch Series 4","ETag":"1","price":7000}
```
##### Пример 12 - Пользователь-администратор не указал значение параметра "select" (включая price и rule)  
Запрос:  
```
GET /v1/ElectronicTypes/25 HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "1"

{"id":25,"name":"Apple Watch Series 4","ETag":"1","price":7000,"rule":"apple_watch_series_4_rules.pdf"}
```
### Метод GET /v1/Rules/{file_name}  
Получить документ с правилами проверки и оценки товаров по его имени file_name.  
При запросе можно использовать заголовок If-None-Match с указанием ETag, чтобы вернуть документ только в том случае, если он был обновлён.  
ETag документа на сервере увеличивается каждый раз при его изменении (например, методом PUT), удалении или добавлении.  
#### Описание запроса  
##### Параметры запроса  
- file_name: string - уникальное название имени документа.  
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- If-None-Match: string, необязательный - принимает значение ETag в двойных кавычках. При указании, делает так, что сервер вернёт документ только если посланный ETag данного документа не совпадает с ETag данного документа на сервере (иначе будет ответ с кодом 304 Not Modified).
##### Тело запроса
Так как это метод GET, то тело запроса следует оставлять пустым.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен)  
- 400 Bad Request - неверный формат заголовков запроса или присутствуют неизвестные параметры запроса, либо запрос не является корректным HTTP запросом  
- 404 Not Found - если документ правил проверки и оценки товаров с данным именем не найден на сервере.  
- 403 Forbidden - пользователь не имеет прав на получение документов (в случае, если ролью пользователя является "клиент", а не "администратор" или "инспектор")  
- 304 Not Modified - при указании заголовка If-None-Match, если значение заголовка совпадает с ETag данного документа правил проверки и оценки товаров на сервере  
- 200 OK - если получилось успешно вернуть данный документ проверки и оценки товаров из системы.  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 200 OK, равен MIME-типу получаемого с сервера документа.  
- Content-Length: integer - размер возвращаемого документа (в байтах)  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.  
- ETag: string - текущий ETag данного вида электроники на стороне сервера (при ответе с кодом 200. Иначе не возвращается). ETag вида электроники представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление документа правил проверки и оценки с заданным file_name.  
##### Тело ответа  
В теле ответа представлен документ, посланный с сервера (если код ответа 200 OK).  
Если код ответа был не 200 OK, то в теле ответа могут содержаться сведения об ошибке, дублирующие или дополняющие код ответа. 
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему.  
Запрос:  
```
GET /v1/Rules/example_file.pdf?unknownParam=true HTTP/1.1

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен).  
Запрос:  
```
GET /v1/Rules/example_file.pdf?unknownParam=true HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии.  
Запрос:  
```
GET /v1/Rules/example_file.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer unknownToken
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент указал неизвестный параметр в строке запроса.  
Запрос:  
```
GET /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found. Request should not contain any parameters"}
```
##### Пример 5 - пользователь-клиент указал имя документа, которого нет в системе.  
Запрос:  
```
GET /v1/Rules/unknown_file.pdf HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 404 Not Found
Content-Type: application/json

{"code":404,"message":"Rules document with presented name not found"}
```
##### Пример 6 - пользователь-клиент попытался получить доступ к существующему в системе документу с правилами.  
Запрос:  
```
GET /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to view this resource"}
```
##### Пример 7 - пользователь-администратор указал заголовок If-None-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 304 Not Modified
Content-Type: application/json

{"code":304,"message":"ETag is equal to If-None-Match value"}
```
##### Пример 8 - пользователь-администратор указал заголовок If-None-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-None-Match: "0"
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Length: 13129
ETag: "1"

<Текст PDF-документа example_file.pdf>
```
##### Пример 9 - пользователь-инспектор получил существующий в системе документ с правилами.  
Запрос:  
```
GET /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Length: 13129
ETag: "1"

<Текст PDF-документа example_file.pdf>
```
### Метод POST /v1/ElectronicTypes
Добавляет новый вид электроники (вместе с суммой выдачи и именем прикреплённого к нему документа с правилами проверки и оценки) из тела запроса в систему.  
Для обеспечения идемпотентности используется заголовок Idempotency-Key со значением UUID v4, сгенерированный клиентом. Ключ идемпотентости и ответ хранится на сервере 24 часа, в ходе которых на запрос POST /v1/ElectronicTypes с данным ключом будет возвращаться сохранённый на сервере ответ (новый вид электроники при этом создаваться не будет).  
Данный запрос доступен только пользователям системы с ролями "Инспектор" и "Администратор".  
#### Описание запроса  
##### Параметры запроса  
Параметры запроса отсутствуют. При попытке передать серверу параметр запроса, сервер вернёт ответ с кодом 400 Bad Request  
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- Idempotency-Key: string (В виде UUIDv4), обязательный - сгенерированный клиентом ключ идемпотентности для данного запроса в формате UUIDv4. Если отсутствует, сервер вернёт ответ с кодом 400 Bad Request.  
- Content-Type: string, обязательный - принимает только значение application/json. В противном случае, будет возвращён ответ с кодом 400 Bad Request.  
- Content-Length: integer, обязательный - принимает значение длины тела запроса в байтах.  В случае указания неправильной длины тела запроса, сервер может неправильно обработать тело запроса и вернуть ошибку 400 Bad Request. В случае указания тела запроса больше 100 000, сервер выдаст ответ с кодом 413 Payload Too Large  
- If-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер создаст новый вид электроники только если посланный ETag совпадает с ETag коллекции видов электроники на сервере (иначе будет ответ с кодом 412 Precondition Failed).  
##### Тело запроса  
JSON объект с несколькими из следующих полей:  
- name: string, обязательное - название вида электроники  
- price: integer, необязательное - сумма денег за сдачу клиентом данного вида электроники  
- rule: string, необязательное - название документа (без URL-encoding) с правилами проверки и оценки техники
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен).
- 403 Forbidden - при попытке пользователем с ролью "Клиент" воспользоваться данным методом.  
- 400 Bad Request - неверный формат заголовков запроса или переданы параметры запроса, либо запрос не является корректным HTTP запросом, либо отсутствует один из заголовков: Idempotency-Key, Content-Type, Content-Length, либо не удалось десериализовать JSON (из-за ill-formed JSON объекта в теле ответа, либо из-за неправильного значения Content-Length).
- 413 Payload Too Large - тело запроса является слишком длинным (>100 000, определяется по заголовку Content-Length)
- 422 Unprocessable Entity - если в переданном серверу JSON объекте присутствуют неизвестные поля, либо известные поля имеют неправильный тип (например, строка вместо числа), либо отсутствует обязательный параметр "name", либо указано название документа, которого нет на сервере.  
- 412 Precondition failed - при указании заголовка If-Match, если значение заголовка не совпадает с ETag данной коллекции на сервере.  
- 201 Created - если получилось успешно создать требуемый элемент коллекции. В теле ответа, при этом, вернётся созданный элемент коллекции.  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 201 Created, равен application/json.
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.
- ETag: string - текущий ETag данного вида электроники на стороне сервера (при ответе с кодом 201. Иначе не возвращается). ETag вида электроники представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление вида электроники по одному id.  
##### Тело ответа  
В случае успешного создания вида электроники, возвращается JSON объект данного вида электроники со следующими полями:  
- id: integer - уникальный идентификатор вида электроники  
- name: string - название вида электроники  
- ETag: string - текущий ETag данного вида электроники 
- rule: string - название документа без URL-encoding с правилами проверки и оценки для данного элемента (если название документа не было указано в теле запроса, то данное поле возвращено не будет)  
- price: integer - сумма денег (в рублях), получаемых клиентом за сдачу данного вида электроники (если сумма для данного вида электроники не была указана в теле запроса, то данное поле возвращено не будет)
В случае ошибки, в теле может быть указана информация, дополняющая или дублирующая код ответа
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему.  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен).  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии.  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer unknownToken
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент попытался добавить новый вид электроники  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to post resource through this method"}
```
##### Пример 5 - пользователь-администратор не указал заголовок Idempotency-Key  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Missing required header Idempotency-Key"}
```
##### Пример 6 - пользователь-администратор указал значение заголовка Idempotency-Key в неверном формате
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: someRandomValue
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Wrong Idempotency-Key header value format: it should be uuidv4 without any extra symbols, e.g. e09d554f-65b5-496e-8515-184a31ec8c3e"}
```
##### Пример 7 - пользователь-администратор указал неизвестный параметр в строке запроса
Запрос:  
```
POST /v1/ElectronicTypes?unknownParam=true HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found in URL! This method does not accept any parameter!"}
```
##### Пример 8 - пользователь-администратор произвел запрос с весом тела больше 100 000 байт
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 100001

<длинное тело запроса>
```
Ответ:  
```
HTTP/1.1 413 Payload Too Large
Content-Type: application/json

{"code":413,"message":"Payload is too large! Please send a request with payload with size of 100 000 bytes maximum"}
```
##### Пример 9 - пользователь-администратор отправил в теле запроса некорректно сформированный JSON
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Cannot deserialize JSON request body!"}
```
##### Пример 10 - пользователь-администратор отправил в теле запроса JSON с неразрешённым полем ETag
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 97

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf","ETag":"23"}
```
Ответ:  
```
HTTP/1.1 422 Unprocessable Entity
Content-Type: application/json

{"code":422,"message":"Cannot create resource. Unknown field \"ETag\" in request JSON! The only allowed field in request JSON are \"name\",\"price\" and \"rule\""}
```
##### Пример 11 - пользователь-администратор отправил в теле запроса JSON без обязательного поля name
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 52

{"price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 422 Unprocessable Entity
Content-Type: application/json

{"code":422,"message":"Cannot create resource. Missing required field \"name\" in JSON request body"}
```
##### Пример 12 - пользователь-администратор отправил в теле запроса JSON, в котором поле rule имеет тип number  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 58

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":15}
```
Ответ:  
```
HTTP/1.1 422 Unprocessable Entity
Content-Type: application/json

{"code":422,"message":"Cannot create resource. Field \"rule\" should be of type string!"}
```
##### Пример 13 - пользователь-администратор отправил в теле запроса JSON, в котором поле rule имеет название документа с правилами, которое не удалось найти на сервере  
Запрос:  
```
POST /v1/ElectronicTypes HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 78

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"unknown_document.pdf"}
```
Ответ:  
```
HTTP/1.1 422 Unprocessable Entity
Content-Type: application/json

{"code":422,"message":"Cannot create resource. Not found document on server with name \"unknown_document.pdf\"!"}
```
##### Пример 14 - пользователь-инспектор указал заголовок If-Match, переданный ETag совпал со значением ETag коллекции на сервере  
Запрос:  
```
POST /v1/ElectronicTypes?unknownParam HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85
If-Match: "3"

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf"}
```
Ответ:  
```
HTTP/1.1 201 Created
Content-Type: application/json
Content-Length: 92
ETag: "1"

{"id":2,"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf","ETag":"1"}
```
##### Пример 15 - пользователь-инспектор указал заголовок If-Match, переданный ETag не совпал со значением ETag коллекции на сервере  
Запрос:  
```
POST /v1/ElectronicTypes?unknownParam HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Idempotency-Key: e09d554f-65b5-496e-8515-184a31ec8c3e
Content-Type: application/json
Content-Length: 85
If-Match: "1"

{"name":"Samsung Galaxy Z Fold 2","price":10000,"rule":"samsung_galaxy_z_fold_2.pdf}"
```
Ответ:  
```
HTTP/1.1 412 Precondition Failed
Content-Type: application/json

{"code":412,"message":"ETag is not equal to If-Match value!"}
```
### Метод POST /v1/Rules/{file_name}  
Загрузить новый документ с правилами проверки и оценки из тела запроса в систему.  
Данный запрос доступен только пользователям системы с ролями "Инспектор" и "Администратор".  
#### Описание запроса  
##### Параметры запроса  
- file_name: string - название и одновременно уникальный идентификатор загружаемого документа с правилами проверки и оценки.
##### Заголовки запроса
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- Content-Type: string, обязательный - должен содержать MIME-тип загружаемого документа. В противном случае, будет возвращён ответ с кодом 400 Bad Request.  
- Content-Length: integer, обязательный - принимает значение длины тела запроса в байтах.  В случае указания неправильной длины тела запроса, сервер может или не загрузить файл совсем(вернет ответ 400 Bad Request), или загрузить усеченный вариант файла. При значении больше 100 000 000, сервер вернет ответ с кодом 413 Payload Too Large.
##### Тело запроса  
В теле запроса должен передаваться загружаемый документ.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен).
- 403 Forbidden - при попытке пользователем с ролью "Клиент" воспользоваться данным методом.  
- 400 Bad Request - неверный формат заголовков запроса или переданы параметры запроса (кроме file_name), либо запрос не является корректным HTTP запросом, либо отсутствует один из заголовков: Content-Type, Content-Length.
- 413 Payload Too Large - тело запроса является слишком длинным (>100 000 000, определяется по заголовку Content-Length).  
- 409 Conflict - документ с правилами проверки и оценки с данным названием уже существует на сервере.  
- 201 Created - если получилось успешно загрузить документ с правилами проверки и оценки.  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 201 Created, равен application/json.  
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.
- ETag: string - текущий ETag созданного документа с правилами проверки и оценки на стороне сервера (при ответе с кодом 201. Иначе не возвращается). ETag документа с правилами проверки и оценки представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление документа с правилами проверки и оценки по одному имени файла.
- Location: string - при успешной загрузке документ (код ответа 201), содержит адрес URL страницы с загруженным документом
##### Тело ответа
В случае успешного создания документа с правилами, возвращается JSON объект, содержащий два поля:
- code: integer - код ответа (201)  
- message: string - пояснение по поводу того, что было сделано в ходе выполнения метода ("New document was created on server!")
В случае ошибки, в теле может быть указана информация, дополняющая или дублирующая код ответа  
#### Примеры
##### Пример 1 - пользователь не указал токен входа в систему.  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен).  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии.  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer unknownToken
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент попытался добавить новый документ с правилами проверки и оценки  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to post resource through this method"}
```
##### Пример 5 - пользователь-администратор указал неизвестный параметр в строке запроса
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found in URL! This method does not accept any parameter except file to upload name!"}
```
##### Пример 6 - пользователь-администратор произвел запрос с весом тела больше 100 000 000 байт
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Content-Type: application/pdf
Content-Length: 100000001

<длинное тело запроса>
```
Ответ:  
```
HTTP/1.1 413 Payload Too Large
Content-Type: application/json

{"code":413,"message":"Payload is too large! Please send a request with payload with size of 100 000 000 bytes maximum"}
```
##### Пример 7 - пользователь - инспектор попытался добавить документ с правилами проверки и оценки, хотя документ с данным именем уже существует в системе  
Запрос:  
```
POST /v1/Rules/apple_iphone_7_rules.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 409 Conflict
Content-Type: application/json

{"code":409,"message":"Cannot post new rule. Rule with this name already exist."}
```
##### Пример 8 - пользователь - инспектор успешно добавил документ с правилами проверки и оценки  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 201 Created
Content-Type: application/json
ETag: "1"
Location: /v1/Rules/new_rule_to_post.pdf

{"code":201,"message":"Succesfully created new document with rules!"}
```
### Метод GET /v1/Rules  
Получть список всех документов с правилами проверки и оценки в системе с их именами и ETag.  
Список сортируется в порядке добавления правила проверки в систему.  
Данным методом могут воспользоваться только пользователи с ролями "Инспектор" или "Администратор"
Если список видов электроники изменится, то возможен пропуск элементов при их постраничном просмотре. Чтобы избежать этого, можно проверять, изменилась ли коллекция в ходе итераций при помощи ETag.  
#### Описание запроса  
##### Параметры запроса  
- skip: integer, необязательный, неотрицательное - число пропущенных с начала списка элементов, при его сортировке в порядке добавления элементов в систему (максимально можно вернуть 100 элементов за запрос, по умолчанию равно 0).  
- maxCount: integer, необязательный, неотрицательное - максимальное число возвращённых с начала списка элементов после пропуска skip, при его сортировке в порядке добавления элементов в систему (максимально можно вернуть 100 (если maxCount больше 100, то всё равно выдастся только 100 элементов) элементов за запрос, по умолчанию равно 10).  
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- If-None-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер вернёт список только если посланный ETag данной коллекции не совпадает с ETag данной коллекции на сервере (иначе будет ответ с кодом 304 Not Modified).  
- If-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер вернёт список только если посланный ETag данной коллекции совпадает с ETag данной коллекции на сервере (иначе будет ответ с кодом 412 Precondition Failed).  
##### Тело запроса  
Так как это метод GET, то тело запроса следует оставлять пустым.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен)
- 403 Forbidden - если пользователь с ролью "Клиент" попытался воспользоваться данным методом    
- 400 Bad Request - неверный формат заголовков запроса или неверное значение параметров запросов (не целое неотрицательное число в случае skip и maxCount), либо неизвестный параметр запроса, либо запрос не является корректным HTTP запросом.  
- 304 Not Modified - при указании заголовка If-None-Match, если значение заголовка совпадает с ETag данной коллекции на сервере  
- 412 Precondition failed - при указании заголовка If-Match, если значение заголовка не совпадает с ETag данной коллекции на сервере  
- 200 OK - если получилось успешно вернуть требуемые элементы коллекции (возможно, их будет 0, при пустой коллекции или при некоторых параметрах skip и maxCount).  
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 200 OK, равен application/json.
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.  
- ETag: string - текущий ETag всей коллекции Rules на стороне сервера (при ответе с кодом 200. Иначе не возвращается). ETag коллеции представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление элемента в коллекции.  
##### Тело ответа  
При коде ответа 200 OK, вернётся JSON объект с 3 полями:  
- count: integer - число возвращённых документов с правилами проверки и оценки.  
- totalCount: integer - суммарное число документов с правилами проверки и оценки в коллекции.  
- items: array - список с возвращёнными видами электроники.  
  
Каждый элемент в массиве items представляет собой объект JSON со следующими полями:  
- name: string - название документа с правилами проверки и оценки  
- ETag: string - текущий ETag данного документа с правилами проверки и оценки  
Иначе, в теле ответа могут содержаться сведения об ошибке, дублирующие или дополняющие код ответа.  
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему. 
Запрос:  
```
GET /v1/Rules?unknownParam=true HTTP/1.1

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен). 
Запрос:  
```
GET /v1/Rules?unknownParam=true HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии. 
Запрос:  
```
GET /v1/Rules?unknownParam=true HTTP/1.1
Authorization: Bearer unknownToken

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент попытался получить список документов с правилами проверки и оценки    
Запрос:  
```
GET /v1/Rules HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08

```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to get resource through this method"}
```
##### Пример 5 - пользователь-инспектор указал неизвестный параметр в строке запроса. 
Запрос:  
```
GET /v1/Rules?unknownParam=true HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909

```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found. Only allowed parameteres are: skip,maxCount"}
```
##### Пример 6 - пользователь-инспектор указал в параметрах skip и maxCount не целое неотрицательное число. 
Запрос:  
```
GET /v1/Rules?skip=-80&maxCount=-80 HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909

```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Wrong skip parameter value. Skip value should be non-negative integer"}
```
##### Пример 7 - пользователь-инспектор указал в параметре maxCount не целое неотрицательное число. 
Запрос:  
```
GET /v1/Rules?maxCount=-80 HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909

```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Wrong maxCount parameter value. MaxCount value should be non-negative integer"}
```
##### Пример 8 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 304 Not Modified
Content-Type: application/json

{"code":304,"message":"ETag is equal to If-None-Match value"}
```
##### Пример 9 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "3"

{"count":1,"totalCount":1,"items":[{"name":"apple_iphone_7_rules.pdf","ETag":"2"}]}
```
##### Пример 10 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "10"

{"count":2,"totalCount":2,"items":[{"name":"apple_iphone_7_rules.pdf","ETag":"1"},{"name":"apple_iphone_8_rules.pdf","ETag":"1"}]}
```
##### Пример 11 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
GET /v1/Rules HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ:  
```
HTTP/1.1 412 Precondition Failed
Content-Type: application/json

{"code":412,"message":"ETag is not equal to If-Match value"}
```
##### Пример 12 - Пользователь-администратор корректно указал значения параметров "maxCount", "skip"  
Запрос:  
```
GET /v1/Rules?skip=10&maxCount=3 HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
```
Ответ:  
```
HTTP/1.1 200 OK
Content-Type: application/json
ETag: "800"

{"count":3,"totalCount":117,"items":[{"name":"google_pixel.pdf","ETag":"1"},{"name":"google_pixel_2.pdf","ETag":"2"},{"name":"google_pixel_3.pdf","ETag":"1"}]}
```
### Метод DELETE /v1/Rules/{file_name}  
Удалить документ с правилами проверки и оценки из системы (ссылка на него у всех связанных с ним типов электроники становится равна null).  
ETag по данному имени файла остается в системе и инкрементируется при удалении.  
Данным методом могут воспользоваться только пользователи с ролями "Инспектор" или "Администратор".  
#### Описание запроса  
##### Параметры запроса  
- file_name: string - имя удаляемого документа с правилами проверки и оценки  
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- If-None-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер удалит документ с правилами только если посланный ETag не совпадает с ETag данного документа на сервере (иначе будет ответ с кодом 304 Not Modified).  
- If-Match: string, необязательный - принимает значение ETag в кавычках. При указании, делает так, что сервер удалит документ с правилами только если посланный ETag совпадает с ETag данного документа на сервере (иначе будет ответ с кодом 412 Precondition Failed).  
##### Тело запроса  
Так как это метод DELETE, тело запроса следует оставлять пустым.
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен)  
- 403 Forbidden - пользователь с ролью "клиент" попытался удалить документ с правилами проверки и оценки  
- 400 Bad Request - неверный формат заголовков запроса, либо неизвестный параметр запроса, либо запрос не является корректным HTTP запросом
- 304 Not Modified - при указании заголовка If-None-Match, если значение заголовка совпадает с ETag данного документа на сервере  
- 412 Precondition failed - при указании заголовка If-Match, если значение заголовка не совпадает с ETag данного документа на сервере
- 404 Not Found - если документа с данным именем не удалось найти на сервере.  
- 204 No Content - если получилось успешно удалить документ.  
##### Заголовки ответа  
- Content-Type: string - содержит MIME-тип тела ответа, если оно присутствует (при коде ответа 204 тело, и, соответственно, данный заголовок, отсутствуют).  
- Content-Length: integer - содержит длину тела ответа (в байтах), если оно присутствует (при коде ответа 204 тело, и, соответственно, данный заголовок, отсутствуют).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.  
##### Тело ответа  
При коде ответа 204 No Content, тело ответа будет пустым.   
Иначе, тело ответа может содержать информацию, дублирующию или дополняющую код ответа.  
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему. 
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен). 
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии. 
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer unknownToken

```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент попытался удалить документ с правилами проверки и оценки  
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08

```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to delete resource through this method"}
```
##### Пример 5 - пользователь-инспектор указал неизвестный параметр в строке запроса. 
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909

```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found. Only allowed parameter is: file_name"}
```

##### Пример 6 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
DELETE /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 304 Not Modified
Content-Type: application/json

{"code":304,"message":"ETag is equal to If-None-Match value"}
```
##### Пример 7 - пользователь-инспектор указал заголовок If-None-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
DELETE /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
If-None-Match: "1"
```
Ответ:  
```
HTTP/1.1 204 No Content

```
##### Пример 8 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag совпал со значением ETag на сервере  
Запрос:  
```
DELETE /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ:  
```
HTTP/1.1 204 No Content

```
##### Пример 9 - пользователь-адмнистратор указал заголовок If-Match, переданный ETag не совпал со значением ETag на сервере  
Запрос:  
```
DELETE /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
If-Match: "10"
```
Ответ:  
```
HTTP/1.1 412 Precondition Failed
Content-Type: application/json

{"code":412,"message":"ETag is not equal to If-Match value"}
```
##### Пример 10 - пользователь-администратор указал несуществющий в системе файл для удаления  
Запрос:  
```
DELETE /v1/Rules/unknown_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba

```
Ответ:  
```
HTTP/1.1 404 Not Found
Content-Type: application/json

{"code":404,"message":"Cannot delete document. Document with rules with this name not found!"}
```

##### Пример 11 - пользователь-администратор указал существующий в системе файл для удаления без проверок на ETag
Запрос:  
```
DELETE /v1/Rules/example_file.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba

```
Ответ:  
```
HTTP/1.1 204 No Content

```
### Метод PUT /v1/Rules/{file_name}  
Заменить документ с правилами проверки и оценки в системе по его имени file_name (или добавить новый, если документа с данным именем нет в системе)  
Данным методом могут воспользоваться только пользователи с ролями "Инспектор" или "Администратор".  
#### Описание запроса  
##### Параметры запроса  
- file_name: string - имя заменяемого документа с правилами проверки и оценки
##### Заголовки запроса  
- Authorization: string, обязательный - принимает только значение "Bearer <токен авторизации>". В противном случае, будет возвращён ответ с кодом 401 Unauthorized.  
- Content-Type: string, обязательный - должен содержать MIME-тип загружаемого документа. В противном случае, будет возвращён ответ с кодом 400 Bad Request.  
- Content-Length: integer, обязательный - принимает значение длины тела запроса в байтах.  В случае указания неправильной длины тела запроса, сервер может или не загрузить файл совсем (вернет ответ 400 Bad Request), или загрузить усеченный вариант файла. При значении больше 100 000 000, сервер вернет ответ с кодом 413 Payload Too Large.  
##### Тело запроса  
В теле запроса должен передаваться загружаемый документ.  
#### Описание ответа  
##### Коды ответа  
- 401 Unauthorized - не предоставлен токен входа в систему, либо токен нерабочий (возможно, токен просрочен).
- 403 Forbidden - при попытке пользователем с ролью "Клиент" воспользоваться данным методом.  
- 400 Bad Request - неверный формат заголовков запроса или переданы параметры запроса (кроме file_name), либо запрос не является корректным HTTP запросом, либо отсутствует один из заголовков: Content-Type, Content-Length.
- 413 Payload Too Large - тело запроса является слишком длинным (>100 000 000, определяется по заголовку Content-Length).  
- 201 Created - если получилось успешно загрузить документ с правилами проверки и оценки с новым именем, которого еще не было в системе.
- 204 No Content - если получилось успешно заменить существующий документ с правилами проверки и оценки.
##### Заголовки ответа  
- Content-Type: string - в случае ответа с кодом 201 Created, равен application/json.  
- Content-Length: integer - содержит длину тела ответа (в байтах).  
- WWW-Authenticate: string - при коде ответа 401 Unauthorized, содержит строку Bearer, указывающую, что нужно авторизоваться и получить токен авторизации для использования метода. Иначе не возвращается.
- ETag: string - текущий ETag созданного или измененного документа с правилами проверки и оценки на стороне сервера (при ответе с кодом 201 или 204. Иначе не возвращается). ETag документа с правилами проверки и оценки представляет собой число, передающееся в виде строки внутри двойных кавычек (напр. "1"), которое инкрементируется на 1 каждый раз, когда происходит изменение или удаление или добавление документа с правилами проверки и оценки по одному имени файла.
- Location: string - при успешной загрузке документ (код ответа 201 или 204), содержит адрес URL страницы с загруженным документом
##### Тело ответа  
В случае успешного создания или изменения документа с правилами, возвращается JSON объект, содержащий два поля:
- code: integer - код ответа (201 или 204)  
- message: string - пояснение по поводу того, что было сделано в ходе выполнения метода ("New document was created on server!" или "Document on server was updated!")
В случае ошибки, в теле может быть указана информация, дополняющая или дублирующая код ответа  
#### Примеры  
##### Пример 1 - пользователь не указал токен входа в систему.  
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Unauthorized. Client needs Authentication through token"}
```
##### Пример 2 - пользователь указал другой метод аутентификации (не через токен).  
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Basic YWxhZGRpbjpvcGVuc2VzYW1l
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Not supported authorization method. The only supported method is \"Bearer\". Client needs Authentication through token"}
```
##### Пример 3 - пользователь указал токен, для которого нет активной сессии.  
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer unknownToken
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 401 Unauthorized
Content-Type: application/json
WWW-Authenticate: Bearer

{"code":401,"message":"Presented token does not have an active session. Please use the new token from server after you authenticate with login and password"}
```
##### Пример 4 - пользователь-клиент попытался загрузить документ с правилами проверки и оценки  
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 087c0099-dee9-46e2-b1c1-2af9cb306e08
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 403 Forbidden
Content-Type: application/json

{"code":403,"message":"Current user do not have rights to put resource through this method"}
```
##### Пример 5 - пользователь-администратор указал неизвестный параметр в строке запроса
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf?unknownParam=true HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 400 Bad Request
Content-Type: application/json

{"code":400,"message":"Unknown parameter found in URL! This method does not accept any parameter except file to upload name!"}
```
##### Пример 6 - пользователь-администратор произвел запрос с весом тела больше 100 000 000 байт
Запрос:  
```
PUT /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 41835905-b1d2-4a76-9141-3803dc1e77ba
Content-Type: application/pdf
Content-Length: 100000001

<длинное тело запроса>
```
Ответ:  
```
HTTP/1.1 413 Payload Too Large
Content-Type: application/json

{"code":413,"message":"Payload is too large! Please send a request with payload with size of 100 000 000 bytes maximum"}
```
##### Пример 7 - пользователь - инспектор успешно заменил существующий документ с правилами проверки и оценки  
Запрос:  
```
PUT /v1/Rules/apple_iphone_7_rules.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 204 No Content
ETag: "3"
Location: /v1/Rules/apple_iphone_7_rules.pdf

```
##### Пример 8 - пользователь - инспектор успешно добавил новый документ с правилами проверки и оценки  
Запрос:  
```
POST /v1/Rules/new_rule_to_post.pdf HTTP/1.1
Authorization: Bearer 90d1a522-7ebd-478f-8507-d57a3e3c3909
Content-Type: application/pdf
Content-Length: 10000000

<Содержимое загружаемого документа>
```
Ответ:  
```
HTTP/1.1 201 Created
Content-Type: application/json
ETag: "1"
Location: /v1/Rules/new_rule_to_post.pdf

{"code":201,"message":"Succesfully created new document with rules!"}
```
## Тестирование API
### Реализация API  
API было реализовано в виде программы, в папке репозитория Lab Work №4/src/ElectronicTypes. Запросы к смежному сервису "Users" были реализованы при помощи заглушек.  

